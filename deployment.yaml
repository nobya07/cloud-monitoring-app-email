apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-app
  namespace: gajendra
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1             
      maxUnavailable: 0 
  selector:
    matchLabels:
      app: flask-app
  template:
    metadata:
      labels:
        app: flask-app
    spec:
      containers:
      - name: cloud-native
        image: gajendra1/cloud-native:latest 
        ports:
        - containerPort: 5000
        
---
apiVersion: v1
kind: Service
metadata:
  name: flask-service
  namespace: gajendra
spec:
  type: LoadBalancer
  selector:
    app: flask-app
  ports:
    - port: 5000
      targetPort: 5000